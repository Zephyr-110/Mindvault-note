<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.community.comment.mapper.CommentMapper">

    <select id="selectByParentId" resultType="org.example.community.comment.entity.Comment">
        SELECT id, post_id, user_id AS author_id, parent_id, content, create_time
        FROM comment
        WHERE parent_id = #{parentId}
        ORDER BY create_time ASC
    </select>

    <select id="selectByPostId" resultType="org.example.community.comment.entity.Comment">
        SELECT id, post_id, user_id AS author_id, parent_id, content, create_time
        FROM comment
        WHERE post_id = #{postId}
        ORDER BY create_time ASC
    </select>

</mapper>